// Bumped by hand on every push — shown at the bottom of Settings so
// it's easy to tell from a phone whether the PWA has picked up the
// latest deploy, without having to go check git. Keep this in sync
// with CACHE_NAME in sw.js so a version bump also busts the SW cache.
const APP_VERSION = "v9 · 2026-08-03";
